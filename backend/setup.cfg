[tool:pytest]
testpaths = tests
addopts = -v --cov=app.core --cov-branch --cov-report=term-missing

[mutmut]
source_paths =
    app/core
also_copy =
    app/schemas
only_mutate =
    app/core/bidding.py
    app/core/wallet.py
pytest_add_cli_args_test_selection =
    tests/core
